# Stack

This is a Next.js (App Router) + Tailwind CSS web app, deployed on Vercel,
backed by Supabase (Postgres, Auth, Realtime). It was originally built as an
Expo/React Native mobile app, then rebuilt as a web app — don't reintroduce
React Native/Expo dependencies.
